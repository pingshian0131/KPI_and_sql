### There are two different function in this package
### KPI compute is used to compute sw checking time 
### sql_monthly is used to dealing with data monly using threadings to decreasing program processing time.
